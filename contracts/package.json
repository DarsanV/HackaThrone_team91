{
  "name": "snapnearn-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for SnapNEarn decentralized traffic violation reporting",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "keywords": ["blockchain", "ethereum", "hardhat", "solidity"],
  "author": "SnapNEarn Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.10.0"
  }
}
